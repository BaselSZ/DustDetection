# dust detection
This project processes satellite TIFF images,
crops a region of interest (Saudi Arabia),
detects dust-like areas using color segmentation,
and exports results as GeoJSON for GIS analysis.

# Can run as:
Docker service<br>
GUI application
